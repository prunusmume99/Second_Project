```
   ┌────────────────────┐
   │            ESP8266 보드                │
   │                                        │
   │             - WiFi 연결                │
   │             - TCP로 JSON 전송          │
   └─────────┬──────────┘
                       │ (WiFi + TCP 소켓)
                       ▼
   ┌──────────────────────────────┐
   │                 🧠 Python TCP Bridge                       │ 
   │                  - socket.accept()                         │
   │                  - 받은 데이터 JSON 파싱                    │
   │                  - ZMQ PUB으로 전송                         │
   └──────────┬──────────────────┘
                         │ (ZMQ PUB)
                         ▼
   ┌──────────────────────────────┐
   │                   🔀 zmq_router.py (중계기)                │
   │                 - XSUB ← 브릿지로부터 수신                 │
   │                 - XPUB → FSM에 중계                       │
   └──────────────┬───────────────┘
                                 │ (ZMQ SUB)
                                 ▼
   ┌──────────────────────────────┐
   │                    ⚙ routine_fsm.py (FSM)                 │
   │                   - ZMQ SUB으로 수신                        │
   │                   - 메시지 기반 상태 처리                   │
   └──────────────────────────────┘
```
